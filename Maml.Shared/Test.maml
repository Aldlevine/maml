class MyApp : Box
{
	void OnFileClick (object sender, Event e)
	{
		// C# Code
	}

	void OnEditClick (object sender, Event e)
	{
		// C# Code
	}

	Box Template =>
	new Box(Name = "Root")
	{
		new VBox()
		{
			new HBox(Name = "TopBar")
			{
				Button(Text = "File", OnClick = OnFileClick)
				Button(Text = "Edit", OnClick = OnEditClick)
			}
			new Box(Name = "Content")
			{
				"This is some text that we are including in here."
			}
		}
	}
}

class Button : Box
{
	void OnPress (object sender, Event e)
	{
		// Set pressed state
	}

	void OnRelease (object sender, Event e)
	{
		// Set released state
	}

	Box Template =>
	new Box()
	{
		
	}
}
